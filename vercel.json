{
  "buildCommand": "pnpm --filter api db:generate && pnpm --filter common build && pnpm --filter web vite:build && cp -r apps/web/dist ./dist",
  "outputDirectory": "dist",
  "installCommand": "corepack enable && pnpm install"
}