# Vercel Environment Variables Configuration
# Copy these variable names to your Vercel dashboard and set appropriate values

# Database (Required)
# Use a production PostgreSQL database (e.g., Supabase, PlanetScale, Railway)
DATABASE_URL=""

# JWT Secrets (Required - Generate strong random secrets)
# Use: openssl rand -base64 32
JWT_SECRET=""
JWT_REFRESH_SECRET=""
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# Server Configuration
NODE_ENV="production"
PORT=3001

# CORS Configuration (Set to your Vercel domain)
# Example: https://your-app.vercel.app
CORS_ORIGIN=""

# Client Configuration (Set to your Vercel API URL)
# Example: https://your-app.vercel.app/api
VITE_API_URL=""